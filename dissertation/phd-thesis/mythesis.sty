%%% -*-LaTeX-*-
%%% This is mythesis.sty.
%%% Put your own private macro definitions, and style modifications,
%%% here.
%%%
%%% However, avoid introducing shorthands for standard LaTeX markup,
%%% such as \beq and \eeq for \begin{equation} and \end{equation},
%%% because it makes your input unreadable to others, and publishers
%%% may reject your LaTeX files because nonstandard markup interferes
%%% with their large-volume production practices.

\newcommand {\mainindex}   [1] {\index{#1|mainpage}}
\newcommand {\mainpage}    [1] {\textbf{#1}}

\newcommand {\gobblearg}   [1] {}

%%% We need a hidden keyword in each index entry to allow extraction
%%% of entries for specialized indexes
\newcommand {\bioname} [1] {\emph {\color {blue}#1}\index{#1@\emph
    {#1}\gobblearg{bioname}}}

\definecoloralias {fsfcolor} {forestgreen}

\newcommand {\fsfname}     [1] {\texttt {\color
    {fsfcolor}#1}\index{#1@\texttt {#1}\gobblearg{fsfname}}}

\newcommand {\figlabel}    [1] {\label{fig:#1}}
\newcommand {\tablabel}    [1] {\label{tab:#1}}

\newcommand {\shortfigref} [1] {{\color
    {utahheadingcolor}\textbf{Figure~\ref{fig:#1}}}}
\newcommand {\figref} [1] {{\color
    {utahheadingcolor}\textbf{Figure~\ref{fig:#1}}} \vpageref{fig:#1}}

\newcommand {\shorttabref} [1] {{\color
    {utahheadingcolor}\textbf{Table~\ref{tab:#1}}}}
\newcommand {\tabref} [1] {{\color
    {utahheadingcolor}\textbf{Table~\ref{tab:#1}}} \vpageref{tab:#1}}

%%% We want \verb and \begin{verbatim} ... \end{verbatim} environments to be
%%% set in color to make them stand out.

\definecoloralias {verbatimcolor} {royalblue3}

\def \verbatim@font {\normalfont \ttfamily \color{verbatimcolor}}

%%% Load package to write normal greek letters in text
\usepackage{textgreek}

%%% Load packages for more powerful tables
\usepackage{booktabs,tabu,multirow}

%%% Load package for subfloats
\usepackage{subcaption}

%%% Define shortcuts for often-used commands
\newcommand{\tn}{\textnormal}
\newcommand{\inv}{^{-1}}
\newcommand{\reals}{\mathbb{R}}

%%% Macros for typesetting derivatives and integrals
\newcommand{\dd}{d}
\newcommand{\Der}[2]{\frac{\dd #1}{\dd #2}}
\newcommand{\Pder}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\Int}[4]{\int_{#3}^{#4} #1 \, \dd #2}

%%% Macros for often-used integrals
\newcommand{\DimReceptorSaturation}{\Int
  {\bondDensity\left(\wallDist,\recAngle,\dTime\right)} {\wallDist}
  {-\infty} {\infty}}
\newcommand{\NDReceptorSaturation}{\Int
  {\ndBondDensity\left(\ndWallDist,\recAngle,\ndTime\right)}{\ndWallDist}
  {-\infty} {\infty}}
\newcommand{\DimQuantity}[1]{\Int {\Int
    {#1 \bondDensity\left(\wallDist,\recAngle,\dTime\right)}
    {\wallDist} {-\infty} {\infty}} {\recAngle} {-\pi/2} {\pi/2}}
\newcommand{\NDQuantity}[1]{\Int {\Int
    {#1 \ndBondDensity\left(\ndWallDist,\recAngle,\ndTime\right)}
    {\ndWallDist} {-\infty} {\infty}} {\recAngle} {-\pi/2} {\pi/2}}

%%% Macro for notes to self
\newcommand{\note}[1]{{\color{red}{\emph{#1}}}}

%%% Input the definitions of model parameters
\input{/Users/andrewwork/thesis/model-defs}
